Tag — small mono-font chip for infrastructure topics (kubernetes, gitops, networking, platform-engineering).

```jsx
<Tag>kubernetes</Tag>
<Tag active>gitops</Tag>
<Tag href="/tags/networking">networking</Tag>
```

`active` highlights the currently-filtered tag (accent fill). Chips wrap in a `flex; gap: var(--space-2)` row — never stack vertically.
